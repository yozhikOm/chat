## 1. Setup: зависимости и прокси

- [x] 1.1 Добавить в `frontend/chatApp/package.json` зависимости `zustand` и `socket.io-client` (версия совместимая с серверной `socket.io ^4.8.3`); проверить `npm install` проходит и пакеты резолвятся
- [x] 1.2 Расширить `vite.config.ts`: добавить прокси `'/socket.io': { target: 'http://localhost:3000', ws: true }` рядом с существующим `/api`; проверить `cd frontend/chatApp && npx vite` стартует без ошибок

## 2. Zustand-стор и типы чата

- [x] 2.1 Создать `src/store/chat.ts`: типы `Channel`, `Message` (переиспользовать/перенести из `api/auth.ts`), интерфейс `ChatState` с полями `channels`, `messages`, `activeChannelId`, `connected`; проверить `npx tsc -b` компилируется без ошибок
- [x] 2.2 Реализовать в сторе экшены: `initFromServer(data)`, `setActiveChannel(id)`, `addChannel(channel)`, `removeChannel(id)` (с переключением на первый оставшийся при удалении активного), `renameChannel(channel)`, `addMessage(message)`, `setConnected(v)`; проверить `npx tsc -b` проходит
- [x] 2.3 Создать `src/api/socket.ts`: фабрику подключения сокета и типизированные обёртки эмитов (`newMessage`, `newChannel`, `renameChannel`, `removeChannel`) с ack-колбэками; проверить `npx tsc -b` без ошибок и экспорты резолвятся

## 3. Интеграция сокета и инициализация данных

- [x] 3.1 Обновить `AppShell.tsx`: при монтировании (после успешой загрузки `/api/v1/data`) вызвать `initFromServer(data)` и установить сокет-подключение; подписаться на события `newMessage`, `newChannel`, `renameChannel`, `removeChannel` и `connect`/`disconnect`, делегируя в экшены стора; на `onLogout` — вызвать `disconnect()` сокета; проверить `npx tsc -b` компилируется
- [x] 3.2 Проверить интеграцию в браузере: вход как `admin`/`admin`, открытие двух вкладок — сообщение из вкладки A появляется во вкладке B в реальном времени (см. README бекенда), `GET /api/v1/data` заполняет список каналов и сообщения

## 4. Панель сообщений

- [x] 4.1 Создать `src/components/MessagePane.tsx`: рендер сообщений активного канала (по `activeChannelId`), при показе истории и потоке `newMessage` не рендерить сообщения других каналов; проверить `npm run build` собирается
- [x] 4.2 Создать `src/components/MessageInput.tsx`: поле ввода с отправкой по Enter/кнопке; не пустое и до 500 символов; при невалидном значении — русскоязычная ошибка под формой, на сервер не отправляется; на submit — эмит `newMessage` через сокет с ack; при ошибке ack — русскоязычная ошибка, сообщение не появляется в чате; проверить `npx tsc -b` без ошибок
- [x] 4.3 Добавить автоскролл вниз в `MessagePane`: `useRef` на контейнер + `useEffect` по числу сообщений активного канала; скролл только когда пользователь не отмотал вручную (порог близости к низу ~40px); проверить в браузере: при добавлении сообщения из другой вкладки контейнер скроллится вниз, при ручной прокрутке вверх — не скроллится
- [x] 4.4 Добавить в `MessagePane` отображение имени автора (username) и текста сообщения, стилизовать через существующие CSS-переменные в `App.css`; проверить `npm run build` и визуально — сообщения чётко читаются на тёмной теме

## 5. Список каналов и модалки

- [x] 5.1 Заменить заглушку в `ChannelList` (сейчас в `AppShell.tsx`): клик по каналу → `setActiveChannel(id)`, активный подсвечен по `activeChannelId`; кнопка «+» рядом с заголовком открывает модалку создания; для каналов с `removable: true` — опции «Переименовать»/«Удалить»; для `removable: false` опций нет; проверить в браузере переключение активного канала меняет сообщения
- [x] 5.2 Создать `src/components/Modal.tsx`: единый общий компонент модалки с пропсами `title`, `children`, `confirmLabel`, `cancelLabel`, `onSubmit`, `onClose`, `danger`, `disabled`, `error`; добавить стили оверлея и карточки в `App.css` через существующие CSS-переменные; проверить `npm run build` чисто собирается
- [x] 5.3 Создать `src/components/ChannelModal.tsx`: форма создания/переименования, рендерится внутри общего `Modal` как `children` (своим оверлеем не владеет); поля имени; надпись кнопки зависит от mode; валидация 2–20 символов с русскоязычной ошибкой при пустом/коротком/длинном имени (на сервер не отправлять); на submit — эмит `newChannel` (create) или `renameChannel` (rename) с ack, при ошибке ack — русскоязычная ошибка; проверить `npx tsc -b` и ручной сценарий создания/переименования
- [x] 5.4 Подтверждение удаления канала: вызывать общий `Modal` напрямую из `ChannelList` (дети — текст вопроса «Вы действительно хотите удалить канал?», кнопка danger, отдельный файл не создаётся); на подтверждение — эмит `removeChannel` с ack, при ошибке ack — русскоязычная ошибка; проверить удаление активного канала переключает на первый оставшийся, удаление неактивного — активный не меняется

## 6. Обработка сетевых сбоев и полировка

- [x] 6.1 Показывать индикатор потери соединения (например, баннер «Нет соединения с сервером») при `connected === false`, не стирая сессию и не разлогинивая; при восстановлении подключения баннер скрывается; проверить в браузере: остановить бекенд → баннер появляется, сессия сохраняется; запустить → баннер исчезает, события продолжаются
- [ ] 6.2 Проверить полный список сценариев спека `chat` по чату в браузере: (a) вход показывает историю и каналы, (b) новое сообщение реального времени, (c) создание/переименование/удаление каналов в двух вкладках, (d) валидация лимитов не отправляет запросы на сервер, (e) сбой сети не разлогинивает
- [x] 6.3 Прогнать `npm run lint` и `npm run build` во `frontend/chatApp/` — чисто без ошибок (синхронизация основной спеки `user-auth` с дельтой происходит при архивации, не в этом change)